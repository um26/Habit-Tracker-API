# --- Database Configuration (Neon/Postgres) ---
# This project uses an external Neon (Postgres) database.
# Provide connection details via environment variables (recommended):
# SPRING_DATASOURCE_URL=jdbc:postgresql://<NEON_HOST>:5432/<DATABASE>?sslmode=require
# SPRING_DATASOURCE_USERNAME=<username>
# SPRING_DATASOURCE_PASSWORD=<password>
# Alternatively, uncomment and set below:
spring.datasource.url=jdbc:postgresql://ep-super-silence-a1xwg5fr-pooler.ap-southeast-1.aws.neon.tech/neondb
spring.datasource.username=neondb_owner
spring.datasource.password=npg_d1OkLHXcxsN9
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# --- Email Configuration (Example using Gmail) ---
# Replace with your actual email provider details
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=devmbandhiya@gmail.com
spring.mail.password=zthncjckzgdhohei
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Email address displayed as the sender
app.email.sender=devmbandhiya@gmail.com

# --- Cloudflare Turnstile Configuration ---
cloudflare.turnstile.secretKey=0x4AAAAAAB7XKMR1VeBOFIkqnzGCKuVfZag
cloudflare.turnstile.siteKey=0x4AAAAAAB7XKG-NBIlgDy55
cloudflare.turnstile.verifyUrl=https://challenges.cloudflare.com/turnstile/v0/siteverify

# --- TOTP 2FA Configuration ---
# Name displayed in the authenticator app
totp.issuer=HabitTrackerApp
app.base-url=https://fuzzy-space-invention-g9wvq957g7gfw56q-8080.app.github.dev

# --- Logging (Optional: Set to DEBUG for more details) ---
logging.level.com.habittracker.api=INFO

server.forward-headers-strategy=framework
server.use-forward-headers=true